spring:
  profiles:
    active: test

  application:
    name: cherry

  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:test;MODE=MYSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        order_inserts: true
        format_sql: true

  docker:
    compose:
      lifecycle-management: start_only

  cloud:
    aws:
      credentials:
        access-key: "test"
        secret-key: "test"
      region:
        static: ap-northeast-2
      s3:
        bucket: cherry-dev-assets
      stack:
        auto: false

cors:
  allowed-origins: http://localhost:3000
  allowed-methods: GET, POST, PUT, DELETE, OPTIONS, HEAD
  allowed-headers: "*"

jwt:
  secret: "crailotestjwtsecretkey2025fordevelopmentandtestingonlylonglonglonglonglonglonglonglonglong"
  access-token-expiration-seconds: 10
  refresh-token-expiration-seconds: 10
  temp-token-expiration-seconds: 10

cookie:
  domain: localhost

kakao:
  client_id: "TestClientId"
  redirect_uri: "http://localhost:8080/callback"